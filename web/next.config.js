/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The experimental appDir feature can be enabled as needed
  experimental: {
    appDir: false,
  },
};

module.exports = nextConfig;